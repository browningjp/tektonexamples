apiVersion: tekton.dev/v1alpha1
kind: Task
metadata:
  name: echo-goodbye-world
spec:
  steps:
    - name: echo
      image: ubuntu
      command:
        - echo
      args:
        - "Goodbye world!"
